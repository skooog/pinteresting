default: &default
 adapter: postgresql
 encoding: unicode
# For details on connection pooling, see rails configuration guide
# http://guides.rubyonrails.org/configuring.html#database-pooling
 pool: 5



development:
 adapter: sqlite3 #should be PostgreSQL
 encoding: unicode
 database: pinteresting_development
 pool: 5
 timeout: 5000
 host: localhost
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# yml hates tabs. hates them.
test:
 adapter: sqlite3
 encoding: unicode
 database: pinteresting_test
 pool: 5
 timeout: 5000
 host: localhost
