# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
adapter: postgresql
encoding: unicode
pool: 5
timeout: 5000

development:
<<: *default
database: "your_development_database_name"

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
<<: *default
database: "your_test_database_name"


#heroku does whatever it wants with production and ignores this file. 
#production:
<<: *default
#database: "your_production_database_name", not sure what goes here 
#pool: 5
#timeout: 5000
